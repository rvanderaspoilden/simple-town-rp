using UnityEngine;

/// <summary>
/// Client-side behaviour for generic furniture props (shelves, lamps, tables…).
/// All state and visual updates (isBuilt, presetId) are handled by PropBehaviourBase.
/// </summary>
public class GenericPropBehaviour : PropBehaviourBase {
}
